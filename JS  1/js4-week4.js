//pd work
/*
In this short Life
That only lasts an house
How much - how little - is
Within our power

My thoughts - true, we can either fight it or go with the flow to make our journey easier.

if(confidence==" High") {
console.log("water high which means positive')
}else if( confidence == "med"){
console.log("water is medium which means neutral exp");
}else{
console.log('water is low which means negative experience');
}

function leakyBucketAnalogy(positiveExperience, selConfidence, difficultOrNegativeExperience) {
  let myBucket = positiveExperience + selConfidence + difficultOrNegativeExperience;
  let selfEsteemLevel = ((positiveExperience + selConfidence) * 100) / myBucket;
  if (selfEsteemLevel >= 66 && selfEsteemLevel <= 100) {
    console.log(Math.round(selfEsteemLevel), "%");
    return `Your self esteem level is Hight`;
  } else if (selfEsteemLevel >= 33 && selfEsteemLevel < 66) {
    console.log(Math.round(selfEsteemLevel), "%");
    return `Your self esteem level is Medium`;
  } else if (selfEsteemLevel >= 0 && selfEsteemLevel < 33) {
    console.log(Math.round(selfEsteemLevel) + "%");
    return `Your self esteem level is Low`;
  }
}
console.log(leakyBucketAnalogy(25, 25, 10))

*/





